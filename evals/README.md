# evals/ — likeness · beauty · perf harness

The measured half of ONE HERTZ. Everything the README claims about this site is produced
here, reproducibly, against a rubric frozen at P0. The harness itself is a portfolio
artifact: prompts, raw judge rationales and frame-time arrays ship with every report.

## One command

```sh
pnpm eval
```

Runs the full round — capture → structural assertions → perf pass → beauty council —
and emits a self-contained HTML report committed at `results/<round>/report.md` (+
`report.html`): side-by-side frame grids, checklist pass/fail, perf traces, judge
verdicts with raw rationales and the exact prompts used.

Partial runs: `pnpm eval --only capture|assert|perf|judge`.

## Layout

```
evals/
├── rubric.yaml          # FROZEN contract (P0): checklist, severities, beauty protocol,
│                        # perf gates, determinism spec. Version-bumped only with
│                        # design-council sign-off; reports cite the version they ran on.
├── capture.ts           # deterministic frame + video capture. Loads ?eval=1, waits for
│                        # fonts.ready + GLB + 3 settled rAF, then addresses frames via
│                        # __ONE_HERTZ__.gotoSection(sectionId, localProgress):
│                        # 70 canonical stills × 2 viewports + ~10 interaction frames,
│                        # plus the scripted 60s scroll videos.
├── assert.ts            # structural checklist runner. Executes every rubric item whose
│                        # assertion is `auto:` against __ONE_HERTZ__.state()/DOM/synthetic
│                        # input; tags failures with rubric severities; enforces the gate
│                        # (zero CRITICAL, ≥90% pass).
├── perf.ts              # in-page rAF delta recorder around lenis.scrollTo(end, ~60s);
│                        # desktop gates + CDP 6× CPU-throttle mobile proxy (GPU-unthrottled
│                        # caveat printed into the report); per-tier via forceQualityTier(n);
│                        # one CDP Performance trace cross-check per round.
├── judge/               # beauty council: pair builder (randomized L/R), judge prompts,
│                        # ballot schema (forced choice per axis + evidence-or-void),
│                        # gate math (win-or-tie ≥60%, no axis <40%, ≥3 axes above source),
│                        # deception probe, inter-judge agreement.
├── reference/
│   ├── source/          # FROZEN P0 capture kit of thewatch.60fps.fr + manifest.
│   │                    # All comparisons run against these files, never the live site.
│   └── ours/            # regenerated by capture.ts every round.
└── results/
    └── <round>/         # e.g. results/r1/ — report.md + report.html + frames/ +
                         # frametimes.json + ballots/ ; committed, linked from README.
```

## Determinism contract

`?eval=1` arms eval mode in the app itself (built in P1): seeded RNG, clock frozen with
the dial at 10:09:30, BPM pinned to 64, ECG phase 0, loader skipped. Capture only begins
after `document.fonts.ready`, the GLB is resident, and 3 consecutive settled rAF frames.
Frame addressing is `(sectionId, localProgress ∈ {0, .25, .5, .75, 1})` through the
section registry — never scroll-pixel offsets — so frames stay stable across layout
changes.

## Gates (summary — rubric.yaml is authoritative)

| Track | Gate |
|---|---|
| Likeness | zero CRITICAL + ≥90% of the 29 binary checklist items |
| Beauty | pairwise vs source: win-or-tie ≥60% overall, no axis <40%, ≥3 axes above source, deception probe passed |
| Perf (desktop) | median ≥55 fps AND p95 ≤22 ms AND ≤5 frames >50 ms per 60 s pass |
| Perf (mobile) | flagship ≥55 fps · mid-tier ≥40 fps (named real device once per round) |

Two-stops ceilings: likeness+beauty 4 rounds, perf 3 rounds — then stop and publish the
deltas (house rule; see `docs/PLAN.md` §6).
