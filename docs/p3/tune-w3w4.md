# P3 lane notes — tune-w3w4 (gate-3 + gate-4 absorption, all 28 items)

Status: **DONE — 28/28 items applied or explicitly handed off, all gates green** · 2026-08-26 · lane: tune-w3w4
Law: `docs/LOOKBIBLE.md` + `docs/p15/motion-bible.md` · contracts untouched · `sections/index.ts` untouched (no registration changes needed).

Verified empirically (not claimed): `npm run build` (tsc strict + vite) clean · **engine-smoke 29/29 ALL PASS · dial-smoke ALL PASS · cursor-smoke ALL PASS** (headless real Chrome via playwright-core `channel:"chrome"`, 1600×900, vite preview **:4591** — own port, :4573/:4599 left to their owners) · zero console errors on every capture pass (full-page `?eval=1` walks + solo sandboxes) · dist token scan clean (`FS 60P`/`Nekst`/`fps-` zero hits) · **evidence refreshed from THIS one final build**: `docs/p2/<S>/solo-{25,50,75}.png` for all 8 wave-3/4 sections + `Footer/solo-100.png` (new) + context frames (`Curves/context-50`, `MWR/contextual-50` + **`contextual-75` (new — the gate demanded truthful .75 evidence)**, `Hands/contextual-50`, `Straps/context-50`, `Images/context-50`, `Colors/contextual-50`, `Parts/contextual-50`, `Footer/context-100`) + **`Hands/sweep/live-p75-rot{185,205,245,265}.png`** (the live-attitude env sweep receipts).

## Context that re-frames both gates (read first)

Gate-3/4 were judged on the build BEFORE the integrate lane's gate-2 tunes landed (gate-3's own continuity note says the dial hot-glow was "still unapplied in this build"). On the current build the **crystal-veil and hot-glow findings for Curves and MWR .25/.5 were already resolved** by `SCREEN_EMISSIVE_INTENSITY 2.8→2.1` — verified on fresh full-page captures before touching anything (Curves-50/MWR-25/50: ink dial, batons with tight halos, HEART RATE 64 legible). What the emissive drop did NOT fix: the **standoff-scale dial wash** (Colors/Parts/Footer — bloom energy dominates when the dial is small on screen) and the **Hands crystal glare** (env reflection, not screen emission). Those two got the real fixes below.

## Applied — gate-3 (13 items)

**Curves** · 1) hot-glow: verified already fixed (evidence above), no double-tune. 2) sub-copy over band: `.crv__scrim` now holds ≥55% porcelain through the copy block's depth (62%→56% @58%, still a §7.1 gradient) + `.crv__card` lifted 9vh→7vh — both lines read on scrimmed porcelain at .5/.75. Bonus (gate-3 continuity #3): eyebrow now `07 · CATALOG 01/03` (18 chars, budget-exact) — the numbered spine is whole again.

**MovementWatchRight** · 1) crystal veil .25/.5: resolved on current build (see context); confirmed on page-truth captures. 2) .75 collapse: rows were already full-ink live (departures at .86); the REAL residue was the anchor-dot cluster + crossing leaders — fixed by re-spreading the case-space anchor offsets (dial (−.06,.56), glass (−.56,.02), bezel (−.38,−.7) in bezel-radius units) → dots sit on their parts with height-ordered, non-crossing leaders at .5 AND .75; `contextual-75.png` now ships so evidence tells the truth. 3) hot-glow: verified. 4) hover cursor chip on row label: **P3 cursor-polish handoff as the gate allows** (row magnetism / dot offset).

**Hands** · 1) crystal glare .5–.92: TWO knobs, both sweep-evidenced. Camera: slide-under `phiOff` −.24→−.12 (settle −.16→−.07). Env: **Hands key `envRotationDeg` 225→245** picked from a **live-attitude rotation sweep at page .75, full-page ?eval=1** (receipts in `docs/p2/Hands/sweep/` — 205 sheets white, 185 cleanest-but-breaks-the-revolution, **245 clean AND keeps the arc monotonic** MWR 200 < 245 < Straps 250). Crystal now reads as glass with a controlled specular; flank/ports/orange button own the frame through .5/.75/.92; the section exits clean. 2) BPM card .75: full-ink 96/220 + captions verified after the light fix — no porcelain chip needed. 3) flank mottling: **look-lane handoff confirmed** (shared GLB baked-texture defect; still visible at 105mm, worst around the speaker ports).

**Straps** · 1) exit collision: family departure .88→.82 (gone by .875, before the base-rig case re-enters at .9) — page-truth .9/.92 show zero text-over-case. 2+4) family rail moved **top-center → bottom-left dead zone** (left 6vw / bottom 9vh, left-aligned): fills the .75 vacancy so the negative space reads authored (band sweeps upper-right, rail anchors lower-left) AND takes the rail off the band entirely — the fuzzy text-shadow halo is deleted, clean ink on bare porcelain. 3) "WOVEN," ghost 31%→**40%** (§7.5 outranks the §4 ghost band over the product) + scrim mid-stop raised to 50% — legible at .25.

## Applied — gate-4 (15 items)

**Shared dial-wash (Colors 1 / Parts 2 / Footer 4) — ONE central fix, zero per-section hacks**: per-section **`bloomStrength` dips on the Colors (.22) / Parts (.25) / Footer (.3) keys in `instrument.json`** — exactly the "drop screen-layer bloom contribution for this section's key" option the gate names, done in the keyframe channel infra-gl already ships (Nocturne 0.85 precedent; per-section retune via look JSON is sanctioned in `docs/p2/infra-gl.md` §2). Result verified at every judged frame: Colors plate .5/.75, Parts card slot all-section, Footer row scale — **ink dials, hour markers + complication rings individually resolvable** (the Nocturne-solo-50 target). Threshold/strength base laws untouched; no code edited for this.

**Images** · 1) hero cell: **replaced the TEMP still with a fresh interim lookdev conversion** — Blender headless re-render of the §6 frame-1 recipe at **1408×2624 native portrait** (film_transparent, Cycles 128spp/OIDN/Metal, derived script in scratchpad; original `grade_and_render.py` untouched) composited over a **4×-supersampled procedurally drawn ink/porcelain wedge** (PIL, LANCZOS) → **zero stair-stepping, zero upscale blur at 1600×900@2** (cell renders at native 2×). `public/assets/gallery/natural-titanium_1.webp` (88 KB). P4's Cycles master remains a drop-in. 2) plate entry: figure rise T0 .1→.16, opacity dur .07→.04, stagger .05→.045 (all landed .48) — the half-faded ink cell that read as a bare grey rectangle at .12 is gone; plate edge enters clean. 3) exit strap-on-grey: **handoff** — the bgStage ramp anchors at section CENTERS in the driver; starting it earlier is keyframe-driver/Nocturne scope, not a Images-module edit (the gate's own "consider" phrasing).

**Colors** · 1) dial wash: shared fix above. 2) DAYBREAK tag: shrunk to text measure (`width:max-content`, padding 2.2rem 7rem) with a both-axes-feathered ellipse at 38% ink — a scrim, not a panel. 3) stray rectangle: root cause = `.col__rail`'s scrim backing visible from p=0; rail now opacity:0 with a timeline fade at .6, plus ALL four Colors scrims re-feathered (ellipses that reach alpha 0 inside their boxes — the old 105–130% gradient sizes clipped at box edges = the "ghost" at .85 too). 4) composition .40–.60: standoff rides 8→6.6 through the reveal window (.32–.46), recenter returns 7.6, press-in 7.1 — the watch is ~15% larger exactly through the grey-line beat.

**Parts** · 1) exit: out-plateau **.82–.97 → .96–1** + an authored **retreat** (.9–1: standoff 10→12.6, aim drifts up-left) so the product leaves by pressing away INSIDE its card slot; the blend-out crossfade lives in the last 4% and starts from a small, lifted watch. Page-truth .9/.97: table fully legible, no sweep-across. 3) picker card rise .18–.28 → **.24–.34** (arrives with rows 1–2, gate's exact numbers). 2) wash: shared fix.

**Footer** · 1) approach seam: the real geometry (found empirically, documented in `footer.ts`) is that the last section's end-clamped window keeps Parts' pin on screen until p=1 and the slate DOM rides (1−p)·vh below its rest — so the gate's ".35 rise / .6 wordmark" numbers still triple-expose. Applied the gate's TARGET over its sample numbers: rise .18→**.35** (lands .51–.66, below the departing table), labels .32→**.52**, slate assembly **.78–.97** (the end card assembles in the final glide; Lenis's 4 s tail paces it), reveals .84–.97. Full-page .3/.5/.6/.75/.9/1: **zero frames of watch-through-table or slate-over-anything**. 2) scrim: constrained to the slate's own band (centered 62rem × 54%, feathered ellipse), opacity timeline-tied to the slate — the lineup rises unwashed. 3) grounding: **per-slot contact shadows shipped** using the §1.4-fix-2 machinery (`createContactShadow` × 4, renamed `footer_slot_shadow_i` so `setHeroVisible`'s name-lookup can't grab one; opacity rides each slot's rise) — the destination frame's four watches sit grounded. 4) wash: shared fix. 5) label continuity: **one canonical name `MILANESE`** (Straps rail renamed; ≤ Footer column width) + unavailable labels dimmed 55%→42% as the honesty cue; the four-Ocean-bands lie stands until the P3 re-skin (founder 2026-08-26: recolor-only colorway axis — P4 reviews the family callout copy).

Plus: **Footer solo runway** (entry side only, Images' pattern) — solo Footer had degenerate bounds (rawStart 0/rawEnd 1, progress pinned at 0; probed empirically); one 100svh runway before the track restores the exact live mapping, so solo evidence now equals page truth (and `solo-100.png` is capturable).

## Deviations / interpretation flags (honest)

1. **`instrument.json` edits (Hands rot 245, bloom dips)** — look DATA, not section modules. Both are the gate items' own named knobs, chosen on sweep/capture evidence, applied in the sanctioned per-section keyframe channel. Flag for the look lane to co-sign.
2. Footer slate at ~.6 per gate → **.78** (geometry above; target language "never overlap" wins over sample numbers).
3. Parts picker .24–.34 straddles the .25 capture (mid-rise at solo-25) — the gate's own numbers; reads as reveal grammar, not a parked card.
4. Curves "07" eyebrow + Footer label dimming are continuity/scope-concern absorptions beyond the 13+15, both one-liners.

## Handoffs (owners after this lane)

- **Look lane**: case-flank baked-texture mottling (Hands 105mm macro worst; visible in MWR/Curves edges) — clean the texture or the Hands framing keeps living with it. Also co-sign the two instrument.json data edits.
- **P3 cursor/polish**: MWR hover chip parks on the row label (dot offset or row magnetism).
- **Keyframe-driver owner**: Images exit "start Nocturne bgStage ramp ~.05 earlier" — needs anchor-shift or an extra key, driver scope.
- **P4**: gallery Cycles masters (hero cell now high-res interim; wedge-AA requirement PROVEN solvable — keep it on P4's checklist), family-callout copy review (founder's recolor-only decision), Fraunces budget check (`immaterial` at ~28px vs §4's ≥40px one-moment rule — pre-existing, not this lane's).
- **P5 council option**: Footer slate could become dual-driven (wall-clock leg on enter like the rise) if the late scrub assembly feels heavy on a slow hand-scroll.

## Pitfalls found this lane (inherit)

1. **Gate verdicts can be stale by one build** — both gates were judged pre-gate-2-tunes; two of the five "worst" findings were already fixed at HEAD. Re-capture the named frames on the CURRENT build before editing anything (three items here needed verification, not code).
2. **The driver's change-caching makes `gl.setEnvRotation` a free live-attitude sweep harness**: full-page `?eval=1` → `gotoSection` → poke rotations → screenshot each. No rebuild per point. (`scratchpad/sweep.ts` pattern; graduated receipts in `docs/p2/Hands/sweep/`.)
3. **Dial wash at standoff is BLOOM energy, not emissive** — the 2.1 emissive fix reads ink close-up but washes at card/row scale; the per-key `bloomStrength` channel is the right per-section knob and costs zero code.
4. **The last section's solo sandbox is degenerate** (unpinned + first + last = both clamps collapse the range). Entry-side runway only — an exit runway would un-clamp the end and break parity with the live end-of-page mapping.
5. **Radial scrims with >100% gradient sizes clip into hard edges** (the "stray rectangle" class of defect). Author ellipses that hit alpha 0 inside the box (≤50% 50% sizes + padding), and keep pre-reveal wrappers at opacity 0 if they carry a backing.
6. Blender+PIL interim pipeline for gallery stills: transparent-film re-render at target aspect + supersampled procedural backdrop = native-res, AA-clean cells without waiting on P4 (few minutes on Metal; derived script stays in scratchpad, source script untouched).
